name: zelrap
description: A new, fun Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # UI
  cupertino_icons: ^1.0.2
  cached_network_image: ^3.0.0

  # State
  freezed_annotation: ^0.14.1

  # Firebase
  firebase_core: ^1.0.2
  cloud_firestore: ^1.0.3
  firebase_messaging: ^9.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  freezed: ^0.14.1+1
  build_runner: ^1.12.2

flutter:
  uses-material-design: true